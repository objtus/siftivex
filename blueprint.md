# Siftivex — 画像アーカイブ検索アプリ 設計メモ

最終更新: 2026-09-03

## 概要

ローカルLLM環境を活用した画像管理・検索アプリ。既存ストック(4万枚超)を対象に、自然言語検索・タグフィルタ・類似画像表示を組み合わせて「探しやすさ」を実現する。ウェブ(ローカル)から閲覧。

## 技術方針

### タグ付け・OCR

| 層 | 役割 | 想定モデル/技術 |
|---|---|---|
| ベクトル化 | 意味的類似検索の土台 | CLIP系(SigLIP, OpenCLIP) |
| タグ付け | ルールに基づく分類 | ローカルVLM(=Vision対応の汎用LLM。専用の画像識別モデルではなく、既存で使っている強いモデルファミリーのVision版を使う想定。例: Qwen系のVL版)。システムプロンプトでタグルールを渡す方式 |
| OCR | 画像内テキスト抽出(多言語対応) | 別途OCRエンジン |

**モデルサイズの方針**: 最初からある程度大きく賢いモデル1本でタグ付けする方針に決定。軽量モデル+低信頼度のみ大きいモデルで再処理、という二段構えは実装・運用コスト(閾値調整、再キュー管理)が増すため見送り。4万枚の初回処理は時間がかかっても一度きりの待ちコストであり、システムの継続的なシンプルさを優先する。二段構えは、運用後に品質のムラを感じてから導入する後乗せの最適化として位置づける。

DB設計イメージ:
```
image_id | embedding(vector) | tags(structured) | ocr_text | vlm_raw_caption | source_path
```

### タグ付け方式の方針

- ルールはシステムプロンプトに渡す方式を採用(コード側のルールエンジンより調整が速い)
- 表記ゆれ対策として「固定タグリストからJSON形式で選択」を強制する
- 判定のブレ対策: temperature を下げる、複数回実行して多数決、などを検討
- タグの階層化は現状イメージなし(見送り)
- 表記ゆれ(「紫系」「パープル系」など)を吸収するエイリアス辞書の必要性は今後の検討課題

### 4万枚の処理方針

- 全件まとめて投入する方針(用途別の優先順位付けはしない)
- 埋め込み・OCRは比較的軽量なので全件先行処理が現実的
- VLMタグ付けは処理時間がかかるため、夜間バッチ等での漸進処理を想定
- 軽量VLMで一次タグ付け→信頼度が低いものだけ高精度モデルで再処理、という二段構えも選択肢

## 用語集

コア概念の「リスト」は他の文脈(タグ一覧、パンくずリスト等)と紛らわしいため、**イメージリスト(ImageList)** という名前に固定する。以降、画像参照の順序付き集合を指す時はこの呼称を使う。

### 概念

| 用語 | 定義 | 実装上の対応(例) |
|---|---|---|
| イメージリスト | 画像参照の順序付き集合。常に画面に表示される「今の作業対象」。全ての表示・操作の中心 | `image_list` |
| フィルタ | イメージリストを絞り込む/生成する操作。タグ・自然言語・OCR・アルバム所属が同列に扱われる | クエリのAND/OR/NOT条件 |
| クエリ | フィルタをテキストで表現したもの。`tag:x AND album:y` のような構文 | SQLite FTS5構文に変換 |
| アルバム | 明示的に保存された画像ID集合への所属を条件とする、特殊なフィルタ。フラット(階層なし)、複数所属可 | `album_members` テーブル |
| 並び替えコンテキスト | フィルタ(集合条件)とは別に、イメージリストの並び順を決める基準。関連度順(自然言語クエリが基準)と類似画像表示(基準画像が基準)の2種 | ソートキー |
| 起点 | 並び替えコンテキストを辿る前の、元のフィルタ条件。小さなスタックで保持し、昇格・復帰ができる | 起点履歴スタック |
| 名前空間タグ | 選択肢が有限で閉じたタグ階層(種類/, 身体部位/, 画角/, アングル/, 人数/) | `namespace/value` 形式 |
| フラットタグ | 階層を持たない自由記述のタグ(組み合わせが無限に近いもの) | `tags` テーブル |
| ルートタグ | 取り込みルート由来の予約タグ(`route:pixiv`等)。フォルダルールにより自動付与 | 予約名前空間 |
| 取り込みプロファイル | フォルダパス単位の取り込みルール設定(ルートタグ+ファイル名パーサー) | `folder_rules.yaml` |
| 差分レイヤー | 自動生成(auto)と手動修正(manual)を分離して保持する方式。タグとOCR両方に適用 | `auto_tags` / `manual_added` / `manual_removed`、`auto_ocr` / `manual_ocr` |
| コンテンツハッシュ | 画像の完全な同一性判定用ハッシュ(BLAKE3等)。ファイル移動検出にも使う | `content_hash` |
| 知覚ハッシュ | 見た目が近い別画像(重複候補)の検出用ハッシュ(pHash) | `phash` |
| missing | 監視フォルダから元ファイルが見つからない状態のフラグ | `status: missing` |
| アーカイブ | 物理削除せず非表示にする状態。フィルタの一種として扱う | `archived: true` |
| 未分類 | VLMのタグ付けが確定しなかった画像に自動付与される状態(タグの一種) | 予約タグ |

### 画面(Screen)

| 画面名 | 内容 |
|---|---|
| 一覧画面(ListScreen) | フィルタバー+イメージリストの表示(マゾンリー/グリッド/詳細リストの3モード切替) |
| 詳細画面(DetailScreen) | 画像本体+タグ編集+OCR編集+類似画像パネル。イメージリスト内の前後移動もここで行う |

類似画像表示は独立画面ではなく詳細画面内のパネル、アルバムも独立画面ではなく一覧画面内の1フィルタとして扱う(専用画面を増やさない方針)。

### UIパーツ(Component)

| コンポーネント名 | 役割 |
|---|---|
| FilterBar | 検索欄+タグ/アルバムチップの行 |
| QueryInput | クエリを直接編集するテキスト欄 |
| TagChip | 循環ボタン(含む/除外/解除) |
| TagPicker | タグ一覧(検索・並べ替え可能) |
| ImageCard | 一覧内の1枚(サムネイル単位) |
| ImageGrid / ImageMasonry / ImageTable | 一覧画面の3表示モードそれぞれ |
| DetailPane | 画像本体+前後移動矢印+位置表示(3/247等) |
| TagEditor | 詳細画面でのタグ追加/削除 |
| OcrEditor | OCRテキストの編集欄 |
| SimilarPanel | 類似画像の軸タブ+結果表示 |
| OriginBreadcrumb | 起点履歴のパンくず表示 |
| BulkActionBar | 複数選択時に現れる一括操作バー(タグ付与・アルバム追加・アーカイブ・再タグ付け) |



設計の中心にあるのは「リスト」という1つのデータ構造。

- **リスト** = 画像参照の順序付き集合。常に画面に表示される「今の作業対象」
- **フィルタリング** = リストを絞り込む/生成する操作。以下はすべて同じ「フィルタ」というカテゴリの中の1種類として統一的に扱う
  - タグ条件
  - 自然言語検索
  - OCR検索
  - **アルバム所属**(通常のフィルタと違い、条件を都度計算するのではなく、明示的に保存された画像ID集合への所属を条件にする点だけが特殊)
- フィルタは重ねがけできる(例: アルバム選択 → さらにタグで絞る → さらに自然言語検索)
- リスト内で複数選択ができ、選択結果に対して「アルバムに追加」「タグ一括付与」などの操作を行う。これらの操作も本質的には同じ(今のリスト内の選択 → 何らかの集合を更新する)
- **詳細画面の前後移動は、その時点のリストの順序をそのまま辿る**。フィルタ・並び順を変えればナビゲーションの順序自体が変わる

### クエリ構文にアルバムを統合

以前決めたタグクエリ構文(`tag:タグ名`, `-tag:タグ名`, AND/OR)に、`album:アルバム名` も同じ並びで書けるようにする。

例: `album:参考資料まとめ AND tag:vaporwave系`

UI上は「アルバムを選ぶ」ボタンがあっても、裏側では同じフィルタ言語に変換される。タグ一覧の循環ボタン(未選択→含む→除外)と同様に、アルバムもリスト形式で選択でき、クエリ欄と双方向に同期する設計を踏襲する想定。

### 全体の画面遷移

アルバム/タグ/自然言語によるフィルタリング ⇔ サムネイル一覧/リスト表示 ⇔ 画像詳細 ⇔ 類似画像表示 を行き来できる構成。詳細画面は左右移動でリスト内の前後画像へ遷移する。

### 検索

- 自然言語検索とタグ検索は別の入力として提供
- 裏側では意味検索(embedding)・タグフィルタ・OCR全文検索をハイブリッドに合成するが、ユーザーには意識させない

### タグによる複雑な条件検索

検討の結果、以下の形に着地:

- **タグ一覧の各行に循環ボタン**(未選択→含む→除外→未選択)を配置。タップで状態が切り替わる
- **クエリ欄は常時直接編集可能なテキスト入力**。`tag:タグ名` 形式の構文(AND/OR/NOT相当の `-tag:`)を直接書ける
- **リストのボタン操作とクエリ欄は双方向に同期**。ボタンを押すとクエリ欄に反映され、クエリ欄を書き換えるとボタンの状態も追従する
- タグ一覧は「利用頻度順」「五十音順」で並べ替え可能、検索フィルタで絞り込み可能(タグ量が多い前提の設計)
- 別ボタンでの「上級者向け構文編集」への切り替えは不要という判断(常時テキストとして表示・編集できる方が良い)
- **制約**: ボタン操作で組み立てられるクエリは「含むタグをAND/ORでまとめ、除外タグをANDで付加する」という単純な構造に限られる。クエリ欄で `(A OR B) AND NOT (C AND D)` のような複雑な入れ子を書いた場合、ボタン側では正確に表現できない
- 上記の制約に対しては、エラー表示ではなく**控えめな注記**(アイコン+グレーテキストで「この条件の一部はリストのボタンに反映されません」)を表示する方針に決定。ボタン側の状態は可能な範囲でベストエフォートに保つ

### 類似画像表示

- 詳細画面から「類似画像」セクションへ。複数の軸で類似性を切り替えられるタブ形式
  - **色味**: CLIP embeddingの近さで十分対応可能と想定
  - **時期**: メタデータ(Exif日付・ファイル作成日)でソートするだけの軽量な実装
  - **モチーフ**: 実装が最も難しい。VLM生成タグの重なりで代用するか、構図特化の専用埋め込みモデルが必要になる可能性

### アルバム(静的リスト)

- 動的アルバム(保存済み検索)は不要と判断。「クエリに一致する画像へのタグ一括付与」で代替可能なため、静的アルバムのみを実装する
- 追加方法: 詳細画面からアルバムを選んで追加/リスト表示で複数選択してまとめて追加、の両方に対応
- **削除方法**: 追加と対称に、詳細画面からアルバムのチェックを外す/アルバム内リストで複数選択してまとめて削除、の両方に対応する。誤操作対策として、前述のUndo(Ctrl/Cmd+Z)がここでも効くようにする
- 並び順: 手動並べ替え可能(使わない場合はデフォルトで追加日順など)
- 画像は複数のアルバムに所属可能
- 階層化はしない(フラットな一覧のみ)。分類が必要な場面はタグ側で担保し、アルバムとタグの役割(集合 / 属性)の重複を避ける
- 構造上はフィルタの一種(`album:アルバム名`)として統一的に扱う(上記「全体のデータ構造」参照)

### パフォーマンス対策(4万枚規模での必須事項)

**バックエンド/DB層**

- ベクトル検索はANN(近似最近傍探索)インデックスが必要。Qdrant/LanceDBはHNSWインデックスを内蔵し、タグ等のメタデータフィルタと組み合わせた「フィルタ付きベクトル検索」を1クエリで実行できる
- タグ・OCR検索はSQLite FTS5の全文検索インデックスを活用(素朴なLIKE検索は避ける)
- クエリ欄への入力はデバウンス(200〜300ms程度)してから検索実行し、キー入力ごとの検索連打を避ける

**フロントエンド層**

- 仮想スクロール必須(react-window, virtua等)。4万件を丸ごとDOM化しない
- サムネイルはインデックス時に事前生成・キャッシュしておき、一覧では小さいサムネイルのみ読み込む。原寸は詳細画面を開いた時だけ取得
- IntersectionObserverによる画像の遅延読み込み

### キーボードショートカット(たたき台)

```
← →       : リスト内の前後移動(詳細画面)
Esc       : リストに戻る
/         : 検索欄にフォーカス
Space     : リストでの複数選択トグル(サムネイル一覧時)
1〜9      : よく使うタグをトグル(割り当ては設定でカスタマイズ可能にする)
A         : 選択中の画像をアルバムに追加
Ctrl/Cmd+Z: 直前の一括操作をUndo
```

### 並び順

- **デフォルト**: ファイル名順
- 詳細リスト(エクスプローラ風)表示ではカラムクリックで名前順・更新日順・タグ数順・ファイルサイズ順に切り替え可能
- アルバム内は手動並べ替え(ドラッグ)、使わない場合はデフォルトで追加日順
- **関連度順**: 自然言語検索(embedding検索)は内部的に「クエリとの近さスコア」を既に計算しているため、新しい概念を持ち込まずに並び順の選択肢として追加できる。自然言語クエリが入力されている時だけ意味を持つため、UI上は自然言語検索が有効な時だけ選択肢に現れる条件付き表示とする(タグのみの絞り込みでは「関連度」という概念自体が存在しないため)

### 並び替えコンテキスト:関連度順と類似画像表示

フィルタ(集合条件、`tag:`/`album:`等)と、並び替えのための「コンテキスト」は分けて考えると、類似画像表示も統一モデルに無理なく組み込める。

- **関連度順**: コンテキスト=自然言語クエリのテキスト。クエリとの距離スコアで並べる
- **類似画像表示**: コンテキスト=基準となる1枚の画像。基準画像とのembedding距離スコアで並べる(色味/時期/モチーフの軸切り替えも、このスコアの計算方法を変えているだけ)

両者は構造的に同じパターン(コンテキストに対するスコアで並べる)であり、クエリ言語上は `similar_to:image_id` のように基準画像をIDで参照する形で統一モデルに組み込める(`album:アルバム名` が名前を参照するのと同じ発想)。

**UI上の工夫**: IDをテキストで直接打つのはユーザーにとって不便なため、既存の「タグの循環ボタン⇔クエリ欄テキスト」の二重表現パターンを踏襲する。クエリ欄の内部表現は `similar_to:img_xxxx` のトークンとして保持する。表示上は画像そのものを埋め込むとゴチャつくため、小さいサムネイル(24px角程度)+ファイル名/連番のテキストラベルという控えめなチップにし、必要な時だけタップでポップオーバー表示する(タグチップと同じ「普段はテキスト、必要な時だけ視覚情報」の発想)。

**計算タイミング**: CLIP embeddingは事前計算済みのため、類似検索自体はLanceDBへのANNクエリで軽い(数ミリ秒オーダー)。ただし詳細画面の前後移動のたびに全軸(色味/時期/モチーフ)を自動計算するのは避け、「類似画像」セクションを実際に開いた時だけ遅延計算する。

**正本リストへの復帰ボタン**: 類似画像を何階層も辿ると元のフィルタ結果から離れていくため、単純な「戻る」(1階層ずつ)とは別に、**最初にリストを開いた時点のフィルタ条件へ直接ジャンプするボタン**を用意する。最初のフィルタ条件はセッション状態等に保持し、類似画像を何回辿っても上書きされないようにする。

**起点を昇格させるボタン**: 上記とは逆方向の操作として、今たどり着いた類似画像ベースの表示(`similar_to:` クエリ)を、正本リストのクエリとして昇格(置き換え)させるボタンも用意する。類似画像を辿っていく中で「こちらの系統の方が本命」となった場合に、そこを新しい起点として探索を続けられる。

起点は小さなスタック(目安: 直近5件程度)として保持し、昇格するたびに積み、溢れたら古いものから捨てる。無制限に履歴を持つ必要はないが、直前の1件だけより多少遡れた方が便利なため。UI上はパンくずリスト形式で表示し、履歴上の好きな地点へ直接ジャンプできるようにする。

これにより、フィルタ(タグ・アルバム)・関連度順・類似画像表示のすべてが、同じクエリ言語と同じUI表現パターンの上に統一的に乗る。

### 一覧表示

3種類の表示モードを切り替え可能にする:

- **マゾンリー(雑誌的)**: 画像を眺める用途。元のアスペクト比を保持して表示。CSS columnsベースは行の高さが不揃いで仮想スクロールと相性が悪く(可変高さ対応の仮想化ライブラリ、例: react-virtuosoが必要)、負荷が比較的高い
- **均一グリッド(サムネイル)**: 正方形寄りに整列した一覧表示。行の高さが一定なので仮想化が単純で軽い
- **詳細リスト(エクスプローラ風)**: 小さいサムネイル+ファイル名/タグ/日付/サイズ等をカラム表示。情報密度重視で、大量の画像から特定の1枚を素早く探す・タグを一括確認/比較する用途に向く。カラムクリックでのソート(名前順・更新日順・タグ数順など)にも対応

リスト表示にはOCR抜粋(先頭30〜40文字)も表示する。

**画面幅によるデフォルトの切り替え**: 狭い画面(スマホ/iPad Split View)では均一グリッドをデフォルトにする(仮想化が軽く、閲覧の主戦場である狭い画面で負荷を抑えられる)。マゾンリーは広い画面(PC)限定のオプション表示とする。

### 詳細画面でのホイール高速切り替え

マウスホイールを回すと画像がどんどん切り替わる操作感を実現する。

- 詳細画面ではホイールイベントをページスクロールではなく前後移動として扱う(`wheel`イベントを検知し`preventDefault`)
- 表示中の画像を軸に前後数枚を常に先読み(プリフェッチ)しておき、高速に操作しても表示が待たされないようにする

**多段階の画像解像度**: 原寸画像をそのまま読み込むと高速切り替え時に確実に詰まるため、3段階に分ける。

```
サムネイル(グリッド用、極小) → 詳細プレビュー(1000〜1500px程度、ホイール切り替えのメイン) → 原寸(拡大・ズーム時のみ遅延読み込み)
```

詳細画面のデフォルト表示は「詳細プレビュー」サイズとし、原寸はズーム操作をした時だけ追加で読み込む。

**キャッシュ**: 直近閲覧した数十枚分をブラウザの画像キャッシュ(またはService Worker)に保持し、前後の行き来でネットワークリクエストが発生しないようにする。

### パフォーマンスのボトルネックになりうる箇所(優先度順)

1. **ブラウザのレンダリング**: 一番体感速度に効く。特にマゾンリー表示の仮想化の難しさ、iPad Split View特有のメモリ・CPU制約(フルスクリーンSafariより重く感じやすい)
2. **ハイブリッド検索のクエリ速度**: SQLite FTS5 + LanceDBの組み合わせ自体は4万件規模なら十分速いが、実装が雑だと(タグ検索とベクトル検索を別々に投げてJS側でマージする、N+1クエリになる等)体感速度が落ちる。バックエンド実装の丁寧さに依存
3. **詳細画面での原寸画像読み込み**: モバイル回線からTailscale経由で見る場合、原寸画像の転送に時間がかかりうる。プログレッシブJPEG/WebP変換、中間解像度プレビューの用意で緩和する

### タグ・OCRの手動修正

**タグ: 差分レイヤー方式**

VLM再判定で人間の修正が消えないよう、上書きではなく差分を重ねる構造にする。

```
auto_tags(VLM生成、再実行のたびに上書き)
+ manual_added(人間が追加、常に保持)
− manual_removed(人間が除外、VLMが再提案しても常に抑制)
= 表示されるタグ
```

- 詳細画面: 1枚単位でタグ追加/削除(manual_added/manual_removedに積む)
- リスト複数選択: 選択画像すべてに対するタグの一括追加/削除
- タグのエイリアス吸収(表記ゆれ対策)は専用辞書システムを組まず、**タグの一括リネーム操作**(例:「紫系」→「パープル系」に全画像で置換)で運用的にカバーする方針

**OCR: 全文上書き方式、位置情報は保持しない**

- `auto_ocr`(自動生成)と `manual_ocr`(人間が上書きした場合はこちらを優先表示)の2層構造。「自動生成に戻す」ボタンあり
- 編集UIはテキストエリアへの全文書き換え。改行区切りにすれば実質的に行単位の修正も可能なため、専用の行単位編集UIは作らない
- OCR結果の用途はフィルタリング(検索)が中心のため、行ごとのバウンディングボックス(画像内の位置情報)は保持しない。画像上でのハイライト表示や部分再OCRなど、位置情報が要る機能は将来的にも見送り

### 対応ファイル形式・画像の同一性

- **対応形式**: jpg/png/webp/gif等の一般的な画像形式を対象とする。PSD・CLIP(Clip Studio Paint)形式のソースファイルは対象外
- **GIF(アニメーション)**: サムネイル・詳細表示ではアニメーションのまま表示する。VLM/CLIP入力用には代表フレーム(先頭 or 中間フレーム)を抽出し、静止画として処理する(元のアニメーションGIFはそのまま保存し、抽出フレームは処理用の中間データとして扱う)
- **画像の同一性identifier**: コンテンツハッシュ(BLAKE3等の高速ハッシュ)を取り込み時に1回だけ計算し、画像の一意な識別子として使う。計算コストは取り込み時のみで無視できる程度。ファイルパスは可変(移動・リネームに対応するため)、ハッシュを主キー相当として扱う
  - 知覚ハッシュ(pHash、既存の重複候補検出用)とは別の役割。コンテンツハッシュ=完全同一性、pHash=見た目の近さ、CLIP embedding=意味的な類似性、と3層で役割分担する

### 移動・削除されたファイルの検出

```
定期スキャン時:
  登録済みパスのファイルが見つからない
    → 同じコンテンツハッシュが監視フォルダ内の別パスに見つかる → 移動と判断、パスのみ更新(タグ等は保持)
    → 見つからない → "missing" フラグを立てる(即削除しない。手動タグ・アルバム所属を失わないため)
    → missing状態が一定期間続いたら、ユーザーに確認を促してアーカイブ/削除を選ばせる
```

### カラーパレット抽出(配色参考用)

取り込み時にk-means等の色量子化で主要色(上位5色程度)をHEX値として抽出・保存する。カラーピッカーでの絞り込みは、選択した色とLab色空間での近さで検索する。優先度は低いが実装コストも低いため、後回しにしやすい機能として位置づける。

### デフォルトの並び順

ファイル名順をデフォルトとする。

### タグの構造:名前空間(閉じたリスト)とフラットタグの併用

全てを階層化するとオントロジー設計が破綻しやすいため、「選択肢が有限で迷わないもの」だけを名前空間(階層)にし、それ以外はフラットタグにする方針。

**名前空間にするもの(閉じたリスト)**

```
種類/     写真, イラスト, 3DCG, 図解/資料, 文章/スクショ, 漫画コマ, ピクセルアート
身体部位/  (具体的なリストは後日詰める)
画角/     全身, 上半身, バストアップ, 顔アップ, パーツクローズアップ
アングル/  正面, 側面, 背面, 俯瞰, あおり
人数/     0人(物・背景のみ), 1人, 2人, 3人以上
```

- VLMにはこの名前空間ごとに固定リストから選ばせる(表記ゆれを抑えるため)
- 美学ジャンル(vaporwave等)は今回の用途(お絵かき参考)では不要と判断し、名前空間から除外

**フラットタグにするもの(組み合わせが無限に近いもの)**

衣装(セーラー服等)、髪型、表情、構図の特徴、素材感など、事前に閉じたリストを作るのが非現実的なものは、階層を持たないフラットタグとして扱う。表記ゆれは既存の「一括リネーム」操作でカバーする。

### 自動インデックス化フロー(n8n)

```
フォルダ監視(トリガー)
  → 新規ファイル検知
  → [即時処理] CLIP embedding 生成 + OCR実行 → DB書き込み
  → [キュー投入] VLMタグ付けジョブを別キューへ(夜間バッチ等で消化)
  → タグ付け完了 → auto_tags を更新
```

embedding/OCRは軽量なため検知後すぐ処理し、VLMタグ付けだけ非同期キューに分離する。この設計により、新規画像はタグ未確定の状態でも意味検索・OCR検索にはすぐ引っかかるようになる。

### 未分類画像のレビュー

専用UIは作らず、既存の統一モデル(フィルタ・リスト・詳細)の上でそのまま実現する。

- 「未分類」も一種のタグとして扱う(VLMが低信頼度だった場合に自動付与)
- レビューは `tag:未分類` のフィルタをかけたリスト表示そのもの
- 作業は詳細画面でタグ修正→前後移動で次の未分類画像へ、の繰り返し(既存の「リストの前後移動」機能を再利用)

### 取り込みルート・フォルダルール

3つの取り込みルート(過去に自作した画像管理ソフトのファイル名タグ形式アーカイブ/pixivダウンローダー形式/Twitter・Google・Pinterest等からの雑多なウェブ収集)を区別して扱う。

- 由来は `route/pixiv` のような予約タグとして自動付与する(既存のクエリ構文にそのまま統合できる)
- フォルダパス単位(孫フォルダ以下も含む前方一致)で「取り込みプロファイル」を設定できるようにする。プロファイルの中身は、①自動付与する固定タグ、②ファイル名パーサー(正規表現+名前付きキャプチャグループで作者名・投稿日・作品ID・pixivタグなどを抽出)
- 設定は単一のYAML等の設定ファイルに保持し、GUIとローカルLLM/エージェントの両方から読み書きできるようにする(GUIはファイルへのフォームに過ぎない構成)。エージェントに「pixivフォルダのルールを作って」と話しかけて既存設定への差分を提案させる、という運用を想定
- パーサー定義(`pixiv_downloader_v1`など)は別ファイルに切り出し、追加・調整をしやすくする

## デバイス・レスポンシブ設計

想定利用シーン: 作画資料としてiPad Split View(縮小ブラウザ)で見る、スマホで見る、PCで見る、の3パターンが混在する。「閲覧・画面遷移のスムーズさ」と「タグ修正等の細かい編集」で最適なUIが異なるため、2段階のブレークポイントで設計方針を分ける。

**狭い画面(スマホ、iPad Split View)= 閲覧・移動が主役**

- 1カラムのシングルビュー。フィルタ→リスト→詳細を画面いっぱいに使って遷移する(これまでのモックアップの設計はこちら側)
- 詳細画面はスワイプで前後移動
- フィルタ/タグ選択はボトムシートにまとめ、普段は画面を圧迫しない
- タップ領域は大きめに。細かい編集の最適化はしない

**広い画面(PC、iPad横向きフル幅)= 編集・一括操作が主役**

- 2〜3ペイン構成(フィルタ/タグ一覧を左に常時表示、中央がリスト、詳細は右か下)
- タグ編集はその場でインライン編集(モーダル遷移なし)
- キーボードショートカットが本領発揮する場面
- クエリ欄の直接編集、複数選択での一括操作もこちらが主戦場

**ブレークポイントの考え方**: iPad Split Viewの幅(概ね320〜500px程度になることが多い)を基準に、「狭い(スマホ/Split View)」「広い(フルスクリーンiPad横向き/PC)」の2段階で切り替える。

## 開発フロー(たたき台)

```
Phase 0: 基盤検証(小規模)
  └ 数百枚程度のサブセットで、CLIP embedding→LanceDB検索、
    VLMタグ付け→SQLite保存、の最小パイプラインを通す
  └ ここで処理時間・精度の肌感を掴む(4万枚に広げる前に)

Phase 1: コアデータ層
  └ DBスキーマ確定(images, tags, albums, embeddings)
  └ n8nでのフォルダ監視→自動インデックス化フロー構築
  └ 4万枚の初回一括投入(embedding/OCR先行、VLMタグ付けは並行して漸進)

Phase 2: 検索・閲覧の最小UI
  └ フィルタ(タグ/自然言語)→グリッド→詳細、の統一モデルをまず動かす
  └ この時点で「実用に耐えるか」を実際に使いながら検証

Phase 3: 編集機能
  └ タグ・OCRの手動修正(差分レイヤー)
  └ アルバム機能
  └ 一括操作(複数選択→タグ付与/アーカイブ/再タグ付け)

Phase 4: 仕上げ・拡張
  └ 類似画像表示(色味/時期/モチーフ)、起点の昇格・履歴
  └ レスポンシブ対応(狭い画面/広い画面の出し分け)
  └ カラーパレット抽出、逆引き画像検索連携
  └ パフォーマンスチューニング(仮想スクロール、クエリ最適化)
```

Phase 0を最初に置く狙いは、4万枚+全機能をいきなり作り込むと「この設計で速度が出るか」が分からないまま進んでしまうため。小規模で検証してから本実装に入ることで手戻りを減らす。

## 逆引き画像検索との連携(出典特定)

**目的の優先順位**: (a)出典・作者を特定して正しいメタデータを補完すること(主目的)、(b)出典が確認できた場合に高解像度版を取得すること(副次的)。

**想定フロー**

```
詳細画面で「ソースを調べる」ボタン
  → SauceNAO(イラスト・アニメ系に強い。pixiv/Twitter/danbooru等を横断)/
     ascii2d(SauceNAOの補助・保険)/
     一般的な画像検索(Google/Bing系。写真・資料系スクショ向け)
     へ画像を送信
  → 候補リンクを提示し、あさんが正しいものを選んで確認
  → 確認された情報をメタデータとして保存: source_url、作者名、投稿日時、作品ID(pixiv等)、元のタグ(あれば)
```

- 出典が確認できたURLがpixivの場合、既存の「取り込みルート」(`route:pixiv`)やpixivダウンローダー形式のファイル名パーサーの仕組みに接続し、高解像度版の再取得や正式なメタデータの取り直しに繋げられる
- Web収集(出典不明)画像に対して最も効果が大きい機能と位置づける

**運用上の制約**

- SauceNAO等のAPIには無料枠のレート制限があり、4万枚への自動一括実行は非現実的。**詳細画面からのオンデマンド操作**として設計する
- `source_url:未確認` のようなフラグを立てておけば、以前決めた「未分類レビュー」と同じ仕組み(フィルタ→リスト→詳細を辿ってレビュー)がそのまま流用できる

## 推奨技術スタック(まとめ)

方針: 新しいサービスをなるべく増やさず、既存のobjtus-server環境(Docker, llama-swap, n8n, Tailscale)に乗せる。1ユーザーのローカル運用なので、マルチユーザー向けの重厚な構成は避ける。

| レイヤー | 推奨 | 理由 |
|---|---|---|
| フロントエンド | React + TypeScript + Vite | hermes agent構想と同じ組み合わせ。仮想スクロール(react-window/virtua)、状態管理(Zustand)等のエコシステムが揃う |
| バックエンドAPI | FastAPI (Python) | minichar-botと同じ構成。VLM/CLIP/OCRのPythonエコシステムと相性が良い |
| メタデータDB | SQLite + FTS5 | 単一ファイルでバックアップが容易。1ユーザー規模ならPostgreSQLの複雑さは不要 |
| ベクトルストア | LanceDB(embedded) | 別サーバーを立てずPythonから直接使え、ANN(HNSW)とメタデータフィルタを組み合わせたクエリに対応 |
| VLM(タグ付け) | 既存のllama-swap経由でQwen系VLモデル | 新しい推論基盤を作らず、既存GPU構成を流用 |
| CLIP埋め込み | open_clip または SigLIP(単体スクリプト) | VLMとは別プロセスなので、余っているGPU(3060/2070 Super)に分離配置すると干渉が減る |
| OCR | PaddleOCR(多言語)+ 必要に応じてmanga-ocr(イラスト内の吹き出し文字向け) | 多言語対応かつ日本語精度が良好。スタイライズされた文字にはmanga-ocrが強い場面がある |
| サムネイル生成 | libvips(pyvips経由) | Pillowより大量バッチ処理が高速 |
| ジョブ管理・自動化 | n8n(既存)+ SQLite上の簡易キューテーブル | 新しいキューシステム(Redis/Celery等)を増やさず、n8nのスケジュール実行+DBのpendingフラグで足りる想定 |
| ファイル保存・同期 | 既存のSyncthing | 元画像フォルダの同期に流用(決定済み) |
| 逆引き画像検索 | SauceNAO API(主)+ ascii2d(補助) | イラスト・アニメ系の出典特定に強い。無料枠のレート制限があるためオンデマンド運用前提 |
| デプロイ | Docker Compose(既存compose群に追加)、Tailscale限定公開 | 既存運用パターンを踏襲。新規に増えるサービスは実質FastAPIコンテナ程度で済む |

## セキュリティ

- 外部公開はせず、Tailscaleネットワーク内限定でのアクセスを基本線とする
- 軽い認証(Basic認証程度)をアプリ自体にかけておく
- フォルダルールのYAML等に認証情報を混在させず、シークレットは別ファイル(.env等)に分離。Syncthing等で同期する場合は同期除外を忘れずに設定する
- SearXNG/Open WebUI/n8nと同居させる場合、ポートやTailscale ACLでこのアプリを別枠にしておき、他サービスの脆弱性の波及を避ける
- 配信セーフモード(ワンクリック非表示機能)は不要と判断
- 実装レベルの細かいセキュリティ対応(バグ対応含む)は、実装フェーズで都度詰める方針とし、現時点では設計を固めすぎない

## アーカイブ性

再生成可能なデータと不可能なデータを切り分けてバックアップ優先度を決める。

| データ | 再生成可否 | バックアップ優先度 |
|---|---|---|
| 元画像ファイル | 不可(特にWeb収集分。元URLを失うと二度と入手できない) | 最優先 |
| 手動修正したタグ・OCR(manual_added/manual_removed/manual_ocr) | 不可 | 最優先 |
| アルバム構成 | 不可 | 最優先 |
| auto_tags・embedding・auto_ocr | 可(再インデックスで復元可能) | 低(3-2-1バックアップの対象外でもよい) |

- 3-2-1バックアップの対象は「元画像+手動編集データ+アルバム」に絞り、embeddingやVLM生成タグ等の再生成可能なデータは対象外として容量を圧縮する
- DBへのロックインを避けるため、タグ・OCRをXMPメタデータやサイドカーJSONとしてもエクスポートできるようにしておく(将来アプリを作り直す際の移行性を確保)
- 元画像フォルダの同期は既存のSyncthing運用をそのまま流用する

## 決定事項(補足)

- **クエリ構文パーサー**: 自作せず、SQLite FTS5(またはPostgreSQL全文検索)の構文をベースに変換する
- **オートコンプリート**: 導入する。`tag:`/`album:` 入力途中で候補をドロップダウン表示
- **リストのOCR抜粋表示**: 導入する。先頭30〜40文字程度を一覧に表示
- **削除・非表示**: 物理削除ではなくアーカイブ(非表示フラグ)方式。`archived:true` として他のフィルタと同様に扱う
- **重複画像検出**: 優先度低。perceptual hash(pHash等)で類似候補をグルーピングして提示するが、自動統合はせず人間が確認してアーカイブする運用
- **一括操作UI**: 新規のUIパターンは作らず、既存の「リスト内複数選択→操作(タグ追加/削除・アルバム追加・アーカイブ・再タグ付け)」の仕組みをそのまま使う。再タグ付けは差分レイヤー方式のおかげで手動修正を保護したまま実行できる

## 未決定・今後の検討事項

- 身体部位名前空間の具体的なリスト
- ファイル名パーサーの具体的な正規表現定義(pixivダウンローダー形式・旧自作アーカイブ形式それぞれ)
- 未分類判定の閾値(信頼度の考え方は当面不要だが、「タグが確定しなかった」をどう定義するかは実装時に詰める)